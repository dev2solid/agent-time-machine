[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "agent-time-machine"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []
[project.scripts]
atm = "agent_time_machine.cli:main"
[tool.setuptools.packages.find]
include = ["agent_time_machine*"]
